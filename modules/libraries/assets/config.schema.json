{
  "type": "object",
  "properties": {
    "offlineMode": {
      "description": "When in offline mode, NPM will not try to use internet to resolve dependencies",
      "type": "boolean"
    },
    "$schema": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "offlineMode"
  ],
  "$schema": "http://json-schema.org/draft-07/schema#"
}

